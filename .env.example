PORT=3000
NODE_ENV=development
DATABASE_URL=

# Upstash Redis (required for merchant data persistence)
# Create a free database at https://upstash.com and copy the REST credentials
KV_REST_API_URL=
KV_REST_API_TOKEN=
KV_REST_API_READ_ONLY_TOKEN=
